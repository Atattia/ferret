# Development and desktop packaging dependencies.
# Tests and benchmarks use unittest and the runtime dependencies.
-r requirements-core.txt
pyinstaller>=6.0

# Optional legacy model export dependencies are installed separately;
# see packaging/export_model.py and .github/workflows/release.yml.
